load("~/My Github Projects/plotTimeSeries/data/arrowSide.rda")
View(arrowSide)
View(arrowSide)
install.packages("magick")
library(magick)
r <- image_read('https://raw.githubusercontent.com/R-CoderDotCom/samples/main/bird.png')
image_scale(r, "200")
source("http://bioconductor.org/biocLite.R")
biocLite("EBImage")
install.packages("spatstat")
library(spatstat)
# artificial image
Z <- setcov(square(1))
View(Z)
M <- as.matrix(Z)
library(magick)
# Step 1: Read the image
image <- image_read("data/arrowSide2.svg")  # Replace with your image path
# Step 2: Convert the image to RGB matrix
image_data <- image_data(image, channels = "rgb")
image_matrix <- as.integer(image_data)
# Step 3: Save the matrix as a .rds file
saveRDS(image_matrix, "data/arrowSide2.rds")
arrowSide2 <- readRDS("~/My Github Projects/plotTimeSeries/data/arrowSide2.rds")
# Install and load necessary packages
install.packages("magick")
install.packages("magick")
# Install and load necessary packages
#install.packages("magick")
library(magick)
# Step 1: Read the image
image <- image_read("data/arrowSide2.svg")  # Replace with your image path
# Step 2: Convert the image to RGB matrix
image_data <- image_data(image, channels = "rgb")
image_matrix <- as.numeric(image_data)
# Step 3: Save the matrix as a .rds file
saveRDS(image_matrix, "data/arrowSide2.rds")
arrowSide2 <- readRDS("~/My Github Projects/plotTimeSeries/data/arrowSide2.rds")
# Step 1: Read the image
image <- image_read("data/arrowSide2.svg")  # Replace with your image path
# Step 2: Convert the image to RGB matrix
image_data <- image_data(image, channels = "rgb")
image_matrix <- as.numeric(image_data)
# Step 3: Save the matrix as a .rds file
saveRDS(image_matrix, "data/arrowSide.rds")
load("~/My Github Projects/plotTimeSeries/data/arrowDown.rda")
View(arrowDown)
# Install and load necessary packages
install.packages("imager")
library(imager)
# Step 1: Load the matrix from the .rda file
load("data/arrowSide.rda")  # Replace with your file path
# Step 2: Convert the matrix to a cimg object
height <- nrow(image_matrix)
width <- ncol(image_matrix)
image_cimg <- as.cimg(image_matrix, dim = c(width, height, 1, 1))  # cimg format: width x height x depth x spectrum
# Step 2: Convert the matrix to a cimg object
height <- nrow(arrowSide)
width <- ncol(arrowSide)
image_cimg <- as.cimg(arrowSide, dim = c(width, height, 1, 1))  # cimg format: width x height x depth x spectrum
# Step 3: Resize the image
new_width <- 100  # Set the desired width
new_height <- 100  # Set the desired height
resized_image <- imresize(image_cimg, size_x = new_width, size_y = new_height)
# Step 3: Resize the image
new_width <- 1000  # Set the desired width
new_height <- 1000  # Set the desired height
resized_image <- imresize(image_cimg, size_x = new_width, size_y = new_height)
# Install and load necessary packages
#install.packages("magick")
library(magick)
# Step 1: Read the image
image <- image_read("data/arrowSide2.svg")  # Replace with your image path
# Step 2: Convert the image to RGB matrix
image_data <- image_data(image, channels = "rgb")
image_matrix <- as.numeric(image_data)
# Step 3: Save the matrix as a .rds file
save(image_matrix, "data/arrowSide.rda")
# Step 3: Save the matrix as a .rds file
save(image_matrix, file = "data/arrowSide.rda")
# Install and load necessary packages
#install.packages("magick")
library(magick)
# Step 1: Read the image
image <- image_read("data/arrowSide2.svg")  # Replace with your image path
# Step 2: Convert the image to RGB matrix
image_data <- image_data(image, channels = "rgb")
image_matrix <- as.numeric(image_data)
# Step 3: Save the matrix as a .rds file
save(image_matrix, file = "data/arrowSide.rda")
load("~/My Github Projects/plotTimeSeries/data/arrowSide.rda")
# Step 1: Read the image
image <- image_read("data/arrowSide2.svg")  # Replace with your image path
# Step 2: Convert the image to RGB matrix
image_data <- image_data(image, channels = "rgb")
image_matrix <- as.numeric(image_data)
# Step 3: Save the matrix as a .rds file
save(image_matrix, file = "data/arrowSide.rda")
load("~/My Github Projects/plotTimeSeries/data/arrowSide.rda")
load("~/My Github Projects/plotTimeSeries/data/arrowDown.rda")
# Step 1: Read the image
image <- image_read("data/arrowSide2.svg")  # Replace with your image path
# Step 2: Convert the image to RGB matrix
image_data <- image_data(image, channels = "rgb")
arrowSide <- as.numeric(image_data)
# Step 3: Save the matrix as a .rds file
save(arrowSide, file = "data/arrowSide.rda")
load("~/My Github Projects/plotTimeSeries/data/arrowSide.rda")
load("~/My Github Projects/plotTimeSeries/data/arrowDown.rda")
dim(arrowSide)
dim(arrowDown)
# Step 1: Read the image
image <- image_read("data/arrowSide2.png")  # Replace with your image path
# Step 2: Convert the image to RGB matrix
image_data <- image_data(image, channels = "rgb")
arrowSide <- as.numeric(image_data)
dim(arrowSide)
load("~/My Github Projects/plotTimeSeries/data/arrowDown.rda")
dim(arrowDown)
# Reshape the 3D array into a 2D matrix
# One approach is to collapse the third dimension (z) into columns
arrowSide <- matrix(arrowSide, nrow = dims[1], ncol = dims[2] * dims[3])
dim(arrowSide)
dims <- dim(arrowSide)
arrowSide <- matrix(arrowSide, nrow = dims[1], ncol = dims[2] * dims[3])
# Check the resulting matrix
print(dim(arrowSide))
# Step 3: Save the matrix as a .rds file
save(arrowSide, file = "data/arrowSide.rda")
